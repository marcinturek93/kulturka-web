import React from 'react';
import {Container} from "react-bootstrap";

import './Header.css'

export const Header = () => {
    return (
        <Container fluid className='Header'>
            Some header
        </Container>
    );
}
